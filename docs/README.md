# docs/ — front-page assets

Drop the demo here, then uncomment the `<!-- DEMO ... -->` block at the top of the
repo [README](../README.md).

- **`brief-dock.png`** — a screenshot of the brief docked beside a live Claude Code
  session (≈900px wide reads well on GitHub), **or**
- **`brief-dock.gif`** — a short loop showing the brief refresh across a couple of
  turns (the most compelling option). If you use a GIF, point the `<img src>` at it.

Capture tip: a side-by-side split (iTerm2/WezTerm/kitty/ghostty) with a brief that
fills the dock shows the value in one frame.
